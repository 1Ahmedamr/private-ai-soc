# Manual Demo Scripts

These are NOT automated tests (no assertions) - they're runnable,
narrated examples used during development to manually verify pipeline
behavior with printed output. Real regression coverage for the same
behaviors lives in tests/unit/. Kept for onboarding/demo purposes -
each shows one specific piece of the pipeline in isolation.
